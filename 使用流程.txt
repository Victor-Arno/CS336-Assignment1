# 步骤 1: 训练 BPE tokenizer（生成 vocab 和 merges）
uv run python demo/train_bpe_tinystories.py

# 步骤 2: 预处理数据（tokenize 并保存为 .bin）
uv run python demo/preprocess_tinystories.py

# 步骤 3: 训练模型
uv run python cs336_basics/train.py \
    --train_path data/tinystories_train.bin \
    --val_path data/tinystories_val.bin \
    --save_checkpoint_path checkpoints/model.pt \
    --vocab_size 10000 \
    ...